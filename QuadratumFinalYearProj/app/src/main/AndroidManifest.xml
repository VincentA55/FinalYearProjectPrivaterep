<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quadratum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/quad_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/quad_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quadratum">
        <activity
            android:name=".CreateNewActivitiy"
            android:exported="false" />
        <activity
            android:name=".ExerciseDetailActivity"
            android:exported="false" />
        <activity android:name=".VerticalListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.quadratum.MainActivity" />
        </activity>
        <activity android:name=".HorizontalListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.quadratum.MainActivity" />
        </activity>
        <activity android:name=".GridListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.quadratum.MainActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>